%p#notice= notice
%h1 Your classes with
%h1= @student.student_profile.name
- @student.class_materials_with(current_teacher).each do |classy|
  Date:
  = classy.date
  %br
  Link:
  = classy.link
  %br
  %br
= link_to 'Back to Dashboard', dashboard_path
