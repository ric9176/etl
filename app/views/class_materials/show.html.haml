.container
  .row
    .col-md-6.col-md-offset-3
      %h1 Your classes with #{@student.student_profile.name}:
      - @student.class_materials_with(current_teacher).each do |material|
        .class
          %h4 Date: #{material.date}
          %h4 Link: #{material.link}
      %h5= link_to 'Back to Dashboard', dashboard_path
